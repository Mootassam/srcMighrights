import { PartnerCategoryModel } from 'src/app/partner-category/partner-category-model';

const { fields } = PartnerCategoryModel;

export default [
  fields.id,
  fields.titleFR,
  fields.description,
  fields.createdAt,
  fields.updatedAt,
];
